# Cosmetics placeholders

These PNG files are placeholders for the premium cosmetics/VIP system.

Replace the files in-place with final production art while keeping the same filenames.
The app references these files through `src/cosmetics/cosmeticAssets.ts`.
